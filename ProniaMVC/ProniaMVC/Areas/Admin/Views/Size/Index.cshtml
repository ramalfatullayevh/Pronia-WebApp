
@{
    ViewData["Title"] = "Index";
}

@model IEnumerable<Size>
<div class="container">
    <div class="card mt-6">
        <div class="table-responsive">
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Edit</th>
                        <th>Delete</th>

                    </tr>
                </thead>
                <tbody>
                    @foreach (Size size in Model)
                    {
                        <tr>
                            <td>@size.Name</td>
                            <td><a type="button" class="btn btn-success" asp-area="Admin" asp-controller="Size" asp-action="Update" asp-route-id="@size.Id">Edit</a> </td>
                            <td><a type="button" class="btn btn-danger" asp-area="Admin" asp-controller="Size" asp-action="Delete" asp-route-id="@size.Id">Delete</a> </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>


